# Optional local LLM. Default UI mode is Local ML; no API key needed.
OLLAMA_URL=http://localhost:11434
OLLAMA_MODEL=llama3.2
# Do not put corporate data, credentials or cloud tokens in this portfolio.
