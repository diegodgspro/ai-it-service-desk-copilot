# Six portfolio demonstration images

These are authored visual summaries generated from actual local engine output, **not screenshots of the Streamlit interface**. They do not show a published GitHub repository. All ticket data is synthetic. No production result is claimed.

1. `01-incoming-preview.png` — incoming VPN incident and scope.
2. `02-analysis-preview.png` — real classification output, P3 and score limitation.
3. `03-troubleshooting-preview.png` — actual runbook steps and knowledge paths.
4. `04-automation-preview.png` — real output of an approved simulation; no service changes.
5. `05-architecture.png` — authored technical architecture diagram.
6. `06-repository-preview.png` — repository capability summary.

Use them in this order for a conventional LinkedIn image post. The supplied post identifies them as demonstration summaries. You can replace previews with genuine UI screenshots after following `../screenshots/README.md`.

To regenerate these visual summaries: install the optional developer dependency Pillow, then run `python scripts/generate_demo_images.py`. Pillow is not needed to run the app.
