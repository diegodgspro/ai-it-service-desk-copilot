# Evaluation artifacts

This directory stores immutable, sanitized outputs from preregistered local
evaluations. It must not contain credentials, identities, absolute paths, cache
locations, model files, or nondeterministic timestamps.
