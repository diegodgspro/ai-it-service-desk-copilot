# Optional local-lab dependency only. Never installed by production or ordinary CI.
# Resolve/install this input in an isolated environment, record the complete
# environment with `pip freeze`, and audit the resolved environment before use.
sentence-transformers==6.0.1
transformers==5.10.4
torch==2.14.0
safetensors==0.8.0
huggingface-hub==1.31.0
